# Supabase-Anbindung (optional) — Werte aus dem Supabase-Dashboard:
# Project Settings → API. Ohne diese Variablen läuft die App im Demo-Modus.
# Lokal als `.env.local` ablegen (ist gitignored); fürs Deployment dieselben
# Werte als GitHub-Actions-Secrets hinterlegen (siehe README).
VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...
